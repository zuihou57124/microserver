spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: SPRINGCLOUD-CONFIG  #配置应用名称
      name: config  #要从远程获取的配置文件名称
      profile: dev  #环境(dev,test,pro)
      label: master

